# Runtime deps for the ROS 2 Humble containers. Python 3.10 (Ubuntu 22.04).
#
# This is deliberately NOT the repo's root requirements.txt. That file targets
# the Python 3.12 training workstation and pins numpy 2.4 and scipy 1.16, both
# of which declare Requires-Python >= 3.11 and will not install here.
#
# scipy is absent on purpose: nothing in the runtime path imports it. The only
# importers are scripts/archive/, which is dead code.
#
# torch and torchvision are NOT listed. The wheel that works is platform
# specific -- CPU on a workstation, the JetPack aarch64 CUDA build on the
# board -- so each Dockerfile installs its own before this file is applied.

numpy==2.1.2
opencv-python-headless==4.12.0.88
pillow==11.0.0
pyyaml==6.0.2
tqdm==4.67.1
psutil==7.0.0
ultralytics==8.3.168
ultralytics-thop==2.0.14
